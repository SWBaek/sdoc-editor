# Copilot instructions

Follow the repository-wide guidance in `AGENTS.md` and the architecture in
`docs/architecture.md`. Apply the scoped files in `.github/instructions/` only
when working in their matching paths.

Keep code, identifiers, and commit messages consistent with the surrounding
source.
