{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://sdoc.dev/schema/sdocbook-v1.0.json",
  "title": "Structured Document Book (.sdocbook)",
  "description": "Book manifest that defines a collection of .sdoc documents for unified export.",
  "type": "object",
  "required": ["sdocBook", "documents"],
  "properties": {
    "sdocBook": {
      "type": "string",
      "description": "Schema version of this book manifest.",
      "enum": ["1.0"]
    },
    "title": {
      "type": "string",
      "description": "Project title used as the main document title in unified export."
    },
    "author": {
      "type": "string",
      "description": "Project author."
    },
    "version": {
      "type": "string",
      "description": "Project version string."
    },
    "counterPolicy": {
      "type": "string",
      "enum": ["continue", "reset"],
      "default": "continue",
      "description": "Continue counters across chapters or reset them at each chapter boundary."
    },
    "documents": {
      "type": "array",
      "description": "Ordered list of .sdoc documents in this project.",
      "items": {
        "type": "object",
        "required": ["path"],
        "properties": {
          "path": {
            "type": "string",
            "description": "Relative path to the .sdoc file from this manifest."
          },
          "label": {
            "type": "string",
            "description": "Optional display label (defaults to filename if omitted)."
          }
        },
        "additionalProperties": false
      },
      "minItems": 1
    }
  },
  "additionalProperties": false
}
